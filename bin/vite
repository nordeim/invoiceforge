#!/usr/bin/env ruby
# bin/vite
# Runs the Vite CLI

ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", __dir__)

require "bundler/setup"
require "vite_ruby"

ViteRuby.install_tasks
load ViteRuby.commands
